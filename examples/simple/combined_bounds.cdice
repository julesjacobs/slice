(* Test discretization with multiple distinct boundaries *)
let x = uniform(0, 1) in
let y = uniform(0, 1) in

(* The comparisons below force x's bag to contain {<0.3, <=0.8} *)
if x < 0.3 then
  x < 0.1
else (
  if x <= 0.8 then
    x < 0.6
  else
    x < 0.9 (* Return a bool *)
)

(* We don't actually care about the result of the if, 
   just that x is compared appropriately. 
   Return y to make the overall type TFloat[] *)
y 