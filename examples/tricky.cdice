let a = uniform(0, 1) in
let b = if a < 0.3
        then uniform(1, 2)
        else uniform(2, 4) in
let c = if b < 1.5
        then a
        else if a < 0.8
             then uniform(3, 5)
             else uniform(5, 7) in
c < 3.7