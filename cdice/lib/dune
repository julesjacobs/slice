(library
 (name contdice)
 (public_name contdice)
 (libraries gsl)
 (modules bag bags ast type_system typed_ast runtime_values types distributions lexer parser parse contdice pretty interp util to_dice))

(ocamllex lexer)

(menhir
 (modules parser))